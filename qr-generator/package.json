{
  "scripts": {
    "format": "prettier . --write",
    "format-check": "prettier . --check",
    "lint": "eslint .",
    "lint-check": "eslint ."
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "eslint": "^9.0.0"
  }
}
